# Copy to .env for local dev (docker-compose reads this automatically), or
# set these directly as Portainer stack environment variables for real
# deployment — see README.md.

DATABASE_URL=postgres://user:password@your-postgres-host:5432/se_runbook
MANAGER_EMAILS=manager1@island.io,manager2@island.io
CORS_ORIGIN=*
PORT=8080
